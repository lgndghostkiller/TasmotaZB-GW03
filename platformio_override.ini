[env:tasmota32-zigbee-tcp]
extends = env:tasmota32-zigbee
build_flags = 
  ${env:tasmota32-zigbee.build_flags}
  -DUSE_TCP_BRIDGE
; Falls du LAN willst (nicht nötig, eher weglassen, sonst wird die Bin größer):
  -DUSE_ETHERNET
  -DUSE_LAN8720
