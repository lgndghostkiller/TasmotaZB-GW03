[env:tasmota32-zigbee-tcp]
extends = env:tasmota32
board = esp32dev
build_flags =
  ${env:tasmota32.build_flags}
  -DUSE_ZIGBEE
  -DUSE_ZIGBEE_EZSP
  -DUSE_TCP_BRIDGE
; Falls du LAN willst (nicht nötig, eher weglassen, sonst wird die Bin größer):
;  -DUSE_ETHERNET
;  -DUSE_LAN8720
